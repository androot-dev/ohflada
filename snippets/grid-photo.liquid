 <div class="col-lg-{{col-lg}} col-md-{{col-md}} col-sm-{{col-sm}} photo px-0">
   <a href="{{ image }}" class="position-relative" data-fancybox="gallery"
   data-caption="{{ caption }}">
    <img loading="lazy" src="{{ image | image_url: width: 465 }}" alt="{{ caption }}" />
    </a>
 </div>